kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: otel-operator-demo
nodes:
  - role: control-plane
    extraPortMappings:
      # Grafana, exposed by the otel-lgtm NodePort service
      - containerPort: 30000
        hostPort: 3000
        protocol: TCP
