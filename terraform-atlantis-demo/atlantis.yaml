version: 3
projects:
  - name: grafana-dashboards
    dir: .
    workspace: default
    terraform_version: v1.5.0
    autoplan:
      when_modified:
        - "*.tf"
        - "dashboards/*.json"
      enabled: true
    apply_requirements:
      - approved

