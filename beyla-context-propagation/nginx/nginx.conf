events {}

http {
    server {
        listen 18080;

        location / {
            proxy_pass http://node-client:3001;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
        }
    }
}